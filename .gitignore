# System files
.DS_Store
Thumbs.db

# Editor backups / temp
*~
*.swp
*.bak
*.tmp
~$*

# 참고 외부 IP 자료 (PDF + .hwp) — 외부 GitHub public 전환 (2026-04-29, 엔트리 #39 / 2026-04-30 .hwp 추가, 엔트리 #54):
# 참고 자료 (타 회사 사업신청서 PDF·한글파일) 는 IP (저작권·기밀) 로 외부 공개 부적합.
# git filter-repo 로 history 에서 일괄 제거 + 본 .gitignore 로 재포함 차단.
# working tree 에는 보존 (참고 자료용 — git 추적 외).
*.pdf
*.hwp

# === HTML 문서화 (Phase E9) ===
# Python 가상환경
.venv/
__pycache__/
*.pyc

# MkDocs 빌드 출력
site/

# Serena 메모리 (운영 자산 외)
.serena/
