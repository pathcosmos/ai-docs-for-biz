# AI 사업계획서 문구 + ASCII 삽화 자동 생성 시스템 — 구현 가능성 진단

> 작성일: 2026-05-12 · 관련 작업로그: 엔트리 #58·#61 · 분류: 메타 (시스템 설계 진단) · 후속 계획 수립의 기준선
>
> 본 문서는 워크스페이스 누적 자산 (44 마크다운·36 방법론·6 패키지 파일럿·MkDocs E9~E14 인프라) 토대로, 사용자 메타 질문 — *"정부지원 사업, 사업계획서, AI 관련 부분 작성 자동화, 제조업 위주, 이 테마를 가지고 사업기획서 문구 자동화 생성과 ascii 형태의 삽화 자동 생성, 시스템 프로젝트 구현 가능할까?"* — 에 대한 사전 진단 결과이다. **6 Explore 에이전트 (1차 3 + 2차 3) 병렬 조사 + 방법론 4.30·4.34 제약 인지** 후 작성.

---

## 1. 핵심 발견 — 콘텐츠 원료 + 생성 UI 의 약 70-80 % 가 이미 존재

> ⚠ **표현 정정 (사용자 점검 반영)**: 초안의 "자동화 인프라 80 % 완성" 은 과장이었음. **정확하게는 "콘텐츠 원료 (276 블록 JSON 1.2 MB) + 생성 UI 의 70-80 % 가 이미 작동"** 이며, **운영 가능한 전체 자동화 시스템의 80 % 가 완성된 것은 아님**. 보안 프록시·chunking·rate limit·로그 정책·품질 검증 등 운영 필수 요소는 아직 미구현. 본 §1 의 표는 "이미 작동 중인 원료·UI" 와 "운영 자동화에 미달하는 격차" 를 명확히 분리한다.

가장 중요한 발견: **부분 생성 UI 가 이미 가동 중**. Phase E9~E14 HTML 빌드 파이프라인에 내장됨. **신규 시스템 설계가 아니라 기존 자산 위의 점진 확장**으로 접근해야 함 (중복 비용 차단). 단 "확장" 의 범위에는 보안·검증·운영 요소가 모두 포함되므로 일정·비용은 단순 추가가 아닌 신규 구현에 가까운 비중을 차지한다.

### 1.1 이미 작동 중인 것

| 구성요소 | 위치 | 상태 |
|---|---|---|
| 블록 추출 엔진 (276 블록) | `hooks/build_templates_data.py` | 매 빌드마다 자동 실행 |
| 구조화 JSON 산출물 (1.3 MB) | `docs/data/templates.json` | id·title·body·category·section·domain·tags·preview 8 필드 |
| 블록 선택 UI 4 모드 (Quick·§매핑·Track·검색) | `docs/generate.md` + `docs/javascripts/generate-template.js` | 클라 사이드 100 % |
| 플레이스홀더 치환 엔진 (8 변수) | `applyPlaceholders()` in JS | 정규식 기반 — `[고객사]·[공정]·[수치]·[기간]·[%]·[LLM모델]·[벡터스토어]·[임계]` |
| 블록 조립 → Markdown 출력 | `buildOutput()` in JS | 텍스트영역 + 클립보드 + .md 다운로드 |
| 선택적 LLM 후가공 | Gemini + Cloudflare Worker / AI Gateway | **단계 ① 최소 구현 완료** — 브라우저 API 키 UI 제거, Worker endpoint 호출로 전환. 운영 배포·chunking·rate limit 은 후속 |
| 양식 스키마 1 종 (DX촉진) | `양식검증_DX촉진_R&D.md` | 7 컬럼 표 50+ 필드 머신 파서블 (단계·연차 이중구조 포함) |
| 인용 그래프 데이터 | `docs/data/crossref.json` | 274 인용점 — 문서 네비게이션용. **블록 의존성 그래프는 아님** |

> [출처: `hooks/build_templates_data.py` + `docs/javascripts/generate-template.js` + `docs/data/templates.json` (E9~E14 빌드 산출물)]

### 1.2 비어 있는 것 (사용자 요청과의 격차)

| 격차 | 영향 |
|---|---|
| **ASCII 삽화 생성 전무** | 사용자 핵심 요청 — 0 % 구현 |
| 양식 스키마 5 종 (DX촉진 외 5 종) | 양식별 필수 섹션 자동 매칭 불가 — 패키지는 8 섹션 공통 골격만 사용 |
| 의존성 / 시퀀싱 엔진 | 블록 순서·전제 블록 강제 불가, 사용자가 수동 선택 |
| 조건부 포함 규칙 | "철강이면 X·Y 블록 자동 포함" 같은 도메인 룰 부재 |
| 고객 프로필 → 입력 자동 채움 | 8 변수 모두 수동 입력 |
| 자동 품질 검증 (6 항 자체평가) | 약 50 % 만 머신 체크 (5·6 항 그렙) — 1-4 항 의미 판단 |

> [출처: `양식검증_DX촉진_R&D.md` §2 (DX촉진 1 종 만 머신 파서블) + `운영_모드_Quickstart.md` §5.2 (6 항 자체평가의 의미 판단 비율)]

---

## 2. 자동화 가능성 — 영역별 현실 진단

방법론 4.30 (에이전트 안전 영역 200-300 줄) · 4.34 (한국어 압축 ≈ 50 %) 제약을 전제로 한 평가.

| 영역 | 가능성 | 근거 / 한계 |
|---|---|---|
| **블록 모듈 조립 + 플레이스홀더 치환** | ✅ 매우 높음 (이미 작동) | 276 블록 + 8 변수 엔진 가동 |
| **고객 프로필 기반 자동 추천** (패키지·시나리오·가이드 자동 매칭) | ✅ 높음 (1-2 주) | 시나리오 카탈로그 40 종 + 도메인 태그 + 패키지 6 종 이미 매칭 입력으로 충분 |
| **200-300 줄 신규 자산 생성** (시나리오·가이드 1 종) | ✅ 높음 (방법론 4.30 확정) | 이미 다수 자산이 에이전트 호출로 신설됨 |
| **LLM 기반 문구 적응 / 본문 재작성** (톤·맥락·수치 정합) | ✅ 높음 (단계별 산정) | 사용자 결정: **Gemini + Cloudflare Worker + Cloudflare AI Gateway**. 단 단순 일정이 아닌 단계 산정 — **(a) Worker 프록시 최소 구현 = 2-4 일** / **(b) 운영 가능 (rate limit·CORS·payload off·chunking·에러 처리·인용 검증) = 1.5-3 주** / **(c) 4 적응 모드 + 검증 완비 = 3-5 주**. 기존 Gemini 호출부 (브라우저 직접) 가 있어 (a) 전환 비용 낮음. 인용 보존·로그 비식별화·chunking 부재가 핵심 난제 |
| **ASCII 삽화 자동 생성 (4 주력 타입)** | 🟡 중간 (2-3 주) | 플로우·트리·간트·매트릭스 — 워크스페이스 ASCII 도식 약 65-75 % 가 이 4 종 |
| **ASCII 삽화 (복잡 공간 배치)** | 🔴 낮음 | C4·mindmap·복잡 아키텍처 — LLM 출력 가변, 검수 필수 |
| **양식 스키마 5 종 추가** (DX촉진 외) | ✅ 높음 (각 양식 1-2 주) | DX촉진 패턴 (8 섹션·7 컬럼·갭 식별) 복제 가능 |
| **800+ 줄 통합 파일럿 완전 자동 생성** | 🔴 낮음 (방법론 4.30 명시 금지) | 톤 일관성·교차 인용·의미 통합 사람 손 필요. **하이브리드 (자동 60-70 % + 사람 통합 30-40 %) 가 현실적 상한** |
| **6 항 자체평가 자동 채점** | 🟡 중간 (1-2 주) | 5·6 항 머신 가능, 1-4 항 의미 판단 LLM 필요 |

> [출처: `방법론_총론.md` §3.4 (4.30 에이전트 출력 한도) + §3.4 (4.34 한국어 압축) + `운영_모드_Quickstart.md` §5.2 (6 항 자체평가)]

---

## 3. 권장 구현 형태 — 3 계층 점진 시스템

기존 인프라 위에 얹는 단계적 확장. **각 계층은 독립적으로 가치를 내며, 다음 계층 없이도 사용 가능**.

### 3.0 권장 진행 순서 (사용자 점검 반영 — 초안의 Tier 1 우선 순서 정정)

초안은 Tier 1 → 2 → 3 순으로 제안했으나, **사용자 검토 결과 보안 프록시 (Tier 2 의 (a) 단계) 가 최우선** 으로 재정렬됨. 이유:

1. **현재 브라우저 직접 호출 = 운영 차단 사유**: 기존 `docs/javascripts/generate-template.js` 는 `apiKey + rawText` 를 `localStorage` 에 둔 채 Google Gemini endpoint 로 직접 호출했다. GitHub Pages 공개 환경에서 API 키 노출 위험 + Google 의 production 웹앱 client-side 키 사용 금지 권고 위반. **이 상태로 Tier 1·3 위에 LLM 기능을 더 얹으면 노출 면이 확대됨**.
2. **이후 Tier 1·3 모두 동일 LLM 경로 공유**: AI 매핑 (Tier 1 후속) · ASCII LLM 폴백 (Tier 3 의 b) 모두 같은 Worker + Gateway 엔드포인트를 사용. 보안 프록시를 먼저 깔아야 후속 작업이 같은 엔진을 안전하게 쓸 수 있음.

**최종 권장 순서**:

| 순번 | 단계 | 일정 | 위험 |
|---|---|---|---|
| ① | **Tier 2 (a) — 보안 프록시 최소 구현** (Worker + Gateway 경유, secret 처리, 브라우저 직접 호출 제거) | 2-4 일 | 낮음 |
| ② | **Tier 1 — 룰 기반 위저드** (LLM 없이 도메인 × 양식 → 블록 매칭, 사람 검토 후 사용자 수정율 ≤ 30 %) | 1-2 주 | 낮음 |
| ③ | **Tier 2 (b) — 운영 가능 수준 보강** (rate limit·CORS·payload logging off·chunking·에러 처리·인용 보존 검증) | 1.5-3 주 | 중간 |
| ④ | **AI 매핑 (Tier 1 의 확장)** — compact index 기반. **`templates.json` 1.2 MB 전체를 LLM 에 보내지 말 것**. id·title·category·domain·tags·section·preview 만 담은 compact index 를 별도 빌드 산출물로 만들고, 본문은 최종 선택 후 조립 | 약 1 주 | 중간 |
| ⑤ | **Tier 3 — ASCII 삽화 deterministic 4 주력 타입** (룰 기반 우선, LLM 폴백은 부차) | 2-4 주 | 중간 |
| ⑥ | **Tier 2 (c) — 4 적응 모드 풀세트 + 톤·인용 검증 자동화** | (3-5 주 누적 추가) | 중간~높음 |

**총 일정**: 단순 Tier 1+2+3 합산 5-9 주 → **현실적으로는 ①~⑤ 만 해도 6-10 주, ⑥ 까지 완비 시 10-14 주**.

**구현 상태 (2026-05-12, 엔트리 #61)**: ① Tier 2(a) 보안 프록시 최소 구현은 코드 기준 완료. `worker/src/index.js`·`worker/wrangler.toml`·`docs/javascripts/llm-client.js`·`docs/generate.md`·`docs/javascripts/generate-template.js` 가 Worker `/api/llm` 경로로 연결됨. 남은 ① 검증은 Cloudflare 계정·Gateway 값 등록 후 `wrangler deploy --dry-run` / 실제 endpoint polish 호출 확인이다.

### Tier 1 — 인테이크 위저드 (1-2 주, 위험 낮음)

- 현 `docs/generate.md` 를 **고객 프로필 기반 위저드**로 확장
- 입력: 고객사·업종(철강·고무·정밀가공 등)·규모·지원사업(DX촉진·스마트공장·디지털경남 등)·핵심 시나리오 1-2 개
- 처리: 룰 엔진이 패키지 1-6 중 1:1 또는 복합 매칭 + 시나리오 카탈로그에서 도메인 태그 일치 선별 + 가이드·모듈 자동 부착
- 출력: 추천 블록 리스트 + 사용자 수동 추가/제거 → 기존 조립 엔진으로 Markdown 출력
- 신규 파일: `docs/data/match_rules.json` (도메인 × 양식 → 블록 ID 매핑), `docs/javascripts/wizard.js`
- **가치**: 어떤 블록을 골라야 하는지 의사결정 시간을 분 단위 → 초 단위로 단축. `운영_모드_Quickstart.md` 5 항 입력 폼이 이걸로 곧장 대체됨

> [출처: `운영_모드_Quickstart.md` §1 (5 항 입력 폼) + `시나리오_카탈로그.md` (40 시나리오 도메인 태그)]

### Tier 2 — Gemini + Cloudflare LLM 엔진 레이어 (단계 분리, 위험 중간)

- 사용자 결정: LLM 엔진은 **Gemini**, 배포·보안 계층은 **Cloudflare Worker + Cloudflare AI Gateway** 로 구성
- **단계 분리 (사용자 점검 반영)**:
  - **(a) 최소 구현 — 2-4 일**: Worker 생성·Gateway 경유·secret 처리·브라우저 직접 호출 제거. **2026-05-12 코드 구현 완료, 배포 검증 대기**
  - **(b) 운영 가능 — 1.5-3 주**: rate limit + origin allowlist + 요청 크기 제한 + 일일 호출 제한 + mode별 max chars + payload logging off + chunking + 에러 처리 + 인용 보존 검증
  - **(c) 4 적응 모드 풀세트 — 3-5 주 누적**: a~d 모드 + 자동 톤·인용 검증
- 목표 아키텍처:

```text
GitHub Pages / MkDocs generate UI
  → Cloudflare Worker /api/llm
  → Cloudflare AI Gateway
  → Google AI Studio Gemini
```

- 기존 브라우저 직접 호출 (Google Gemini endpoint + `localStorage` API 키) 은 제거하고 Worker 프록시 호출로 대체
- 1 차 모델 기본값: `gemini-2.5-flash` — Cloudflare AI Gateway Google AI Studio provider 예시와 호환. **2026-10-16 deprecation 예정**이므로 **모델명은 반드시 Worker `env.GEMINI_MODEL` 로 분리**하고 `gemini-3-flash-preview`·`gemini-3.1-flash-lite` 등 차세대 경로를 코드 변경 없이 전환 가능하게 유지. 단순 권장이 아닌 **필수 설계 요건**
- Worker secret / env:
  - `GEMINI_API_KEY` — Google AI Studio API 키 (Cloudflare secret)
  - `CF_ACCOUNT_ID`, `CF_GATEWAY_ID` — AI Gateway 경로 구성
  - 선택: `CF_AIG_TOKEN` — authenticated gateway 또는 BYOK 사용 시
- 4 종 적응 모드:
  - **a. 플레이스홀더 → 자연 문장 변환** — `[수치 30 %]` → "기존 대비 30 % 개선되어"
  - **b. 시나리오 특화 재작성** — 공통 블록을 특정 공정·업종 어휘로 미세 조정
  - **c. 인용 보존형 자연화** — `> [출처: …]` 표기를 변형하지 않고 본문 흐름만 정리
  - **d. 수치 정합성 검증** — 가설 시나리오 KPI 와 본문 수치 충돌 탐지
- 신규 인터페이스: `POST /api/llm`
  - Request: `{ "mode": "polish" | "map" | "ascii", "text": "...", "inputs": {...}, "metadata": {...} }`
  - Response: `{ "text": "...", "model": "gemini-2.5-flash", "usage": {...} }`
- 구현 파일: `worker/wrangler.toml`, `worker/src/index.js`, `docs/javascripts/llm-client.js`; 후속 프롬프트 분리 후보: `prompts/*.md` (5-10 종)
- AI Gateway 설정 필수값:
  - rate limit 활성화 — 공개 GitHub Pages 에서 호출되므로 비용 폭주 방지
  - **payload logging 비활성화 필수** — `cf-aig-collect-log-payload: false` (Cloudflare 문서상 로그·payload 저장은 기본 동작에 포함될 수 있음). 고객사명·공정·수치가 Gateway 로그에 저장되지 않도록 차단. **본 프로젝트에서는 "권장" 이 아닌 필수 조건**
  - caching 은 자유 작성 본문에는 기본 비활성. 동일 프롬프트 반복이 많은 내부 테스트·템플릿 검증에만 선택 적용
- **Worker 공개 표면 보강 (필수)**: GitHub Pages 에서 `/api/llm` 호출하면 Worker URL 은 사실상 공개됨. AI Gateway rate limit 만으로는 1 차 방어. 추가로 **(i) origin allowlist** (GitHub Pages 도메인만 허용) **(ii) 요청 크기 제한** (예: 32 KB·64 KB) **(iii) 일일 호출 제한** **(iv) mode 별 max chars** (`polish` 12 K·`map` 4 K·`ascii` 8 K 식 mode별 상한) 적용
- **Chunking / 블록 단위 polish (필수)**: 현 `docs/javascripts/generate-template.js:387-389` 는 `rawText > 100,000` 자면 "블록 줄이세요" 로 거부만 함 (chunking 미구현). Tier 2 (b) 에서는 **블록 단위로 잘라 polish** 또는 **§ 단위 chunking + 재조합** 을 표준 경로로 둠. 긴 본문은 비용·지연·출력 누락 위험이 크므로 LLM 1 회 호출에 80 K 자 같은 input 을 직접 보내지 않음
- **Stage 4 AI 매핑은 compact index 기반 (필수)**: `docs/data/templates.json` 은 약 1.2 MB. Gemini 에 전체 본문을 보내 매핑시키는 방식은 비용·지연·누락 위험 큼. **id / title / category / domain / tags / section / preview 만 담은 compact index** (`docs/data/templates_index.json` 신설 예정) 를 만들고, LLM 은 후보 ID 만 추천 → 본문은 최종 선택 후 클라이언트에서 조립
- **`.gitignore` 보강 (완료)**: `.dev.vars*`·`.env*` 를 차단. `wrangler.toml` 은 secret 값을 담지 않는 배포 설정이므로 추적 대상으로 유지
- **가치**: 25-40 % 신규 작성 부분의 초안 자동화. **단 사람 최종 검수 필수**

> [출처: `방법론_총론.md` §3.1 (4.1 모듈 재사용 60-70 % + 신규 25-40 %) + `가이드_한국_sLM_활용.md` §2.1 (민감도 라우팅·NER 필터) + Cloudflare AI Gateway Google AI Studio provider (`https://developers.cloudflare.com/ai-gateway/usage/providers/google-ai-studio/`) + Cloudflare Worker secrets (`https://developers.cloudflare.com/workers/configuration/secrets/`) + Google Gemini API key security (`https://ai.google.dev/gemini-api/docs/api-key`)]

### Tier 3 — ASCII 삽화 모듈 (2-4 주, 위험 중간)

- **사용자 점검 반영**: 일정 2-3 주 → **2-4 주 (deterministic 4 종 + 테스트 fixture 포함)**. LLM 폴백 품질까지 보장하려면 추가 필요. "90 % 즉시 사용" 은 초기 목표로는 높음 — **초기 80 % 목표 + 1 차 이터레이션 후 90 %** 로 단계화
- 하이브리드 접근:
  - **a. 룰 기반 4 주력 타입** (`docs/javascripts/ascii_*.js`)
    - 플로우차트 (박스 + 화살표 `──→`, `↓`)
    - 트리/계층 (`├──` `└──` indent-based)
    - 간트 (단계+연차 가로 막대)
    - 매트릭스 (`┌─┐` `└─┘` RACI 격자)
  - **b. LLM 폴백** — 위 4 종에 안 맞으면 Tier 2 의 Gemini Worker 경로에 "ASCII 도식 N×M 격자, 박스드로잉 문자만, 100 자/줄 이하" 제약 프롬프트
- 입력 스키마: JSON (`{type, nodes, edges, ...}`) 또는 자연어 ("3 단계 파이프라인 → 모니터링 → 재학습 루프")
- 출력: 마크다운 코드 펜스 안의 ASCII (HWP/DOCX 붙여넣기 가능, 모노스페이스 폰트 호환)
- 신규 파일: `docs/javascripts/ascii_gen.js`, `templates/ascii_*.txt`
- **현실적 한계**: 워크스페이스 ASCII 도식의 약 65-75 % 가 4 주력 타입으로 커버. 나머지 25-35 % (복잡 공간 배치) 는 SVG 유지 또는 사람 재그리기. **현 워크스페이스는 이미 Mermaid → SVG 전환 완료 (Phase E14)** 라 신규 ASCII 의 주된 가치는 **HWP·DOCX 평문 환경 호환**.

> [출처: 워크스페이스 ASCII 도식 분류 결과 (10 파일 ≈ 8-12 도식) + Phase E14 Mermaid→SVG 전환 (115 → 101 SVG 자산)]

---

## 4. MVP 외 (의도적 제외)

- **양식 스키마 5 종 확충** — 신규 양식 발견 시점에 점진 (이미 진행 중인 운영 패턴, 자동화 자체로 분리할 필요 낮음)
- **800+ 줄 통합 파일럿 완전 자동** — 방법론 4.30 위반, 시도 금물
- **일반 백엔드 서비스화** — DB·인증·이력관리까지 갖춘 별도 SaaS 백엔드는 MVP 외. 단, **LLM API 키 보호용 Cloudflare Worker 프록시** 는 Tier 2 필수 범위에 포함
- **PDF/HWP 직접 출력** — 사용자가 Markdown 으로 받아 자체 도구로 변환하는 현 흐름이 더 유연

---

## 5. 인프라 비용 추정 (LLM 호출 기준)

방법론 4.34 한국어 압축 ≈ 50 % 전제. 패키지 1 종 약 5,200 줄 / 117 KB ≈ 80 K 토큰 출력 기준.

| 모델 | 1 패키지 생성 비용 | 비고 |
|---|---|---|
| Gemini 2.5 Flash + Cloudflare Worker/Gateway | 낮음 (Google AI Studio 과금 + Cloudflare Gateway/Worker 사용량) | **1 차 채택안**. 키 노출 제거 + rate limit + analytics 가능. 2026-10-16 전 모델 전환 점검 필요 |
| Gemini 3 계열 (Flash/Pro) + Cloudflare Gateway | 중간 | 후속 전환 후보. 모델 안정성·가격·Gateway 호환성 확인 후 기본값 승격 |
| Claude API 직접/병행 | 중간~높음 | 대안 후보. 이번 사용자 결정 범위에서는 제외하되, 품질 비교 실험용으로만 보류 |
| Gemini 1.5 Flash 브라우저 직접 호출 (legacy) | 낮음 | **제거 완료**. production 웹앱 API 키 client-side 노출을 막기 위해 Worker 경로로 전환 |

정확한 비용은 Google AI Studio/Gemini 과금 tier 와 Cloudflare plan 에 따라 달라진다. 본 워크스페이스는 연간 수십 건 수준의 내부 작성 지원이므로, 비용보다 **키 보호·로그 비식별화·rate limit** 이 1 차 설계 기준이다.

---

## 6. 위험 / 제약 (반드시 사용자 인지 필수)

1. **양식 변경 주기 (연 1 회)** — 정부 사업 양식이 매년 미세 변경. 자동화 후 첫 양식 변경 시 룰·스키마 동기화 작업 필수
2. **인용 무결성** — LLM 적응 시 `> [출처: …]` 표기가 변형·누락되는 사례 발생. **후처리 검증기 필수** (Tier 2-c 모드 + 그렙 감사)
3. **수치 환각** — 가설 KPI 와 본문 수치가 LLM 적응 중 어긋남. Tier 2-d 모드가 이를 잡아야 함
4. **고객사 데이터 유출** — Gemini 호출 자체는 외부 LLM 전송이므로 [고객사]·[공정]·[수치] 치환 후 전송 여부를 사용자가 명시 인지해야 함. Tier 2 에서 **Cloudflare Worker 프록시 + 민감 단어 NER 필터 + AI Gateway payload logging 비활성화** 를 필수화
5. **API 키 노출** — Google 은 production 웹앱에서 client-side API 키 사용을 금지 권고. 기존 localStorage API key UI 는 제거 완료. Gemini 키는 Cloudflare Worker secret 또는 gitignored `worker/.dev.vars` 로만 취급
6. **AI Gateway 로그 정책** — AI Gateway 는 기본적으로 요청·응답 payload 를 로그에 저장할 수 있음. 고객사 데이터 보호를 위해 `cf-aig-collect-log-payload: false` 또는 Gateway 로그 비활성화를 기본값으로 둠
7. **모델 수명주기** — `gemini-2.5-flash` 는 1 차 안정 기본값이나 Google deprecation 일정상 **2026-10-16 shutdown 예정**. 모델명은 반드시 Worker env 로 분리하고 `gemini-3-flash-preview`·`gemini-3.1-flash-lite` 테스트 경로를 미리 열어둬야 함 (코드 변경 없이 모델 전환)
8. **LLM 품질 검증 과소평가** — 초안의 "톤 일관성 80 점 / 인용 100 % 보존" 은 단발 평가 기준. 실제로는 (i) 평가 데이터셋 (블록 30-50 종 × 도메인 3-6 종) 구축 + (ii) 사람 평가 1 차 + 자동 그렙 감사 2 차 + (iii) regression 회피용 fixture 가 필요. **품질 검증 작업량이 Tier 2 (c) 의 3-5 주 중 절반 이상을 차지** 할 수 있음
9. **긴 본문 chunking 부재** — Phase 1 은 `MAX_LLM_CHARS = 12000` 단일 호출 한도로 거부. chunking 또는 블록 단위 polish 가 Tier 2 (b) 의 필수 항목 (위 §3.0 ③). 미구현 상태로 운영 진입 시 패키지 1·4·6 같은 1,000+ 줄 통합 산출물에 LLM 적용 불가
10. **Worker 공개 표면** — GitHub Pages 에서 호출되는 Worker URL 은 사실상 공개. AI Gateway rate limit 만으로는 비용 공격 차단 불완전. §3 Tier 2 (b) 의 4 보강 (origin allowlist·요청 크기·일일 호출·mode max chars) 모두 적용 필수
11. **Stage 4 AI 매핑 size limit** — `templates.json` 1.2 MB 전체를 LLM 에 보내지 말 것. compact index (`templates_index.json`) 로 후보 ID 만 받은 뒤 본문은 최종 조립 단계에서 클라이언트가 처리
12. **secret 누출 위험** — `.dev.vars*`·`.env*` 차단은 완료. 운영 key 는 여전히 Cloudflare secret 으로 등록해야 하며, 채팅·문서·커밋에 값이 남지 않게 관리 필요
13. **검증 책임** — 최종 사업계획서 책임은 작성자에게 있음. **자동화는 초안 가속에 한정**한다는 명확한 인지 필수

> [출처: `가이드_한국_sLM_활용.md` §2.1 (민감도 라우팅·NER) + `방법론_총론.md` §3.3 (인용 무결성·검증 책임 규범) + Cloudflare AI Gateway logging (`https://developers.cloudflare.com/ai-gateway/observability/logging/`) + Gemini API key security (`https://ai.google.dev/gemini-api/docs/api-key`) + Gemini deprecations (`https://ai.google.dev/gemini-api/docs/deprecations`)]

---

## 7. 핵심 검증 파일 (다음 단계 시작 전 읽을 곳)

- `hooks/build_templates_data.py` — 블록 추출 로직 (재사용 가능)
- `worker/src/index.js` — `POST /api/llm` Worker 프록시 구현. Google AI Studio Gateway `v1/models/{model}:generateContent`, `cf-aig-collect-log-payload: false`, origin allowlist, mode/size 가드 확인
- `worker/wrangler.toml` — required secrets (`GEMINI_API_KEY`, `CF_ACCOUNT_ID`, `CF_GATEWAY_ID`) 과 기본 `GEMINI_MODEL`, `ALLOWED_ORIGINS`
- `docs/javascripts/generate-template.js` — `MAX_LLM_CHARS = 12000` 단일 polish 한도. **chunking 미구현 상태의 증거**. Tier 2 (b) 의 표준 chunking 또는 블록 단위 polish 가 이를 대체해야 함
- `docs/data/templates.json` — **1.2 MB** / 276 블록 구조화 데이터. **AI 매핑에 전체 본문 직접 전송 금지** — compact index (`templates_index.json` 신설) 사용
- `.gitignore` — `.dev.vars*`·`.env*` 차단 완료. `wrangler.toml` 은 secret 없는 설정 파일로 추적
- `docs/generate.md` — Worker endpoint UI. Gemini API key 입력 UI 제거 완료
- Cloudflare 공식 문서 (사용자 점검 출처):
  - AI Gateway Google AI Studio provider — `https://developers.cloudflare.com/ai-gateway/usage/providers/google-ai-studio/`
  - AI Gateway logging / payload header — `https://developers.cloudflare.com/ai-gateway/observability/logging/`
  - Worker secrets — `https://developers.cloudflare.com/workers/configuration/secrets/`
- Google 공식 문서 (사용자 점검 출처):
  - Gemini API key security — `https://ai.google.dev/gemini-api/docs/api-key`
  - Gemini deprecations (2.5-flash 2026-10-16 shutdown) — `https://ai.google.dev/gemini-api/docs/deprecations`
- `양식검증_DX촉진_R&D.md` — 양식 스키마 1 종 (5 종 추가 시 복제 패턴)
- `운영_모드_Quickstart.md` — 현 수동 워크플로 (Tier 1 위저드의 대체 대상)
- `방법론_총론.md` §3.4 (4.30 에이전트 출력 한도) / §3.4 (4.34 한국어 압축) — 자동화 안전 영역 / 한국어 압축 비율

---

## 8. 결론 / 사용자 의사결정 대기 사항

**구현 가능. 현실성 사용자 평가 7/10 — 기술 방향은 맞으나 일정과 "80 % 완성" 표현은 낙관적이었음.**

정확한 표현:
- **콘텐츠 원료 (276 블록 JSON 1.2 MB) + 생성 UI 의 70-80 % 가 이미 존재** — 운영 가능한 전체 시스템 완성도는 아직 그 수준 아님
- **점진 확장이지만, 보안·검증·운영 요소는 신규 구현에 가까운 비중** — 단순 추가가 아님
- **현실적 일정** — §3.0 권장 순서 ①~⑤ 완수 = **6-10 주**, ⑥ (4 적응 모드 풀세트 + 자동 검증) 까지 = **10-14 주**. 초안 "5-9 주" 는 (b) 운영 가능 수준 + 검증·chunking·rate limit 을 과소 산정한 결과
- **가장 먼저 할 일** = Tier 2 의 (a) 보안 프록시 (2-4 일) — **코드 구현 완료, Cloudflare 배포·실호출 검증 대기**. 이후 Tier 1 룰 위저드 → AI 매핑 compact index → Tier 3 ASCII deterministic → Tier 2 (c) 풀세트 순

본 진단 후 사용자가 결정해야 할 사항 (후속 계획 수립 시점):

1. **착수 범위** — ① 최소 구현은 완료. 다음은 ② 룰 기반 위저드로 갈지, ③ 운영 보강 (rate limit·chunking·검증) 을 먼저 보강할지 결정
2. **LLM 제공자 / 배포 방식** — ✅ 결정 완료: Gemini + Cloudflare Worker + Cloudflare AI Gateway
3. **Tier 2 세부 정책** — `cf-aig-collect-log-payload: false` 강제 / origin allowlist 도메인 / 일일 호출 상한 / mode 별 max chars / 모델 기본값 (env var 로 분리 — `gemini-2.5-flash` 시작 → `gemini-3` 계열 전환 경로) 최종 확인
4. **양식 스키마 확충** — Tier 1 와 병행 / 별도 사이클?
5. **품질 검증 데이터셋 구축 시점** — Tier 2 (c) 풀세트 작업의 절반 이상을 차지할 가능성. 별도 트랙으로 분리할지 결정 필요
6. **방법론 후보 4.82·4.83·4.84** 의 정식 채택 여부 (작업로그 엔트리 #58 배운점 절 참조)

---

## 9. Verification (계획 승인 후 착수 시)

- 본 진단 자체에는 검증 단계 불필요 (read-only 분석 결과)
- 실제 Tier 착수 시 각 단계 (§3.0 권장 순서 기준) 검증 기준:
  - **단계 ① Tier 2 (a) 보안 프록시**: 브라우저 코드에 Gemini API 키 0 건 / `localStorage.geminiApiKey` 호출 0 건 / Worker `env.GEMINI_API_KEY` secret 기반 호출 성공 / Gateway 경로 정상 로그 / `cf-aig-collect-log-payload: false` 요청 헤더 확인 / `.gitignore` 에 `.dev.vars*`·`.env*` 차단 항목 존재
  - **단계 ② Tier 1 위저드**: 6 패키지 모든 도메인 (철강 대기업·냉연·특수강관·고무·정밀가공·유틸) 에서 위저드가 합리적 블록 세트 자동 추천 (사람 검토 후 사용자 수정율 ≤ 30 %)
  - **단계 ③ Tier 2 (b) 운영 보강**: origin allowlist 외 도메인 호출 거부 / 요청 크기 한도 초과 시 4xx / 일일 호출 상한 초과 시 4xx / mode별 max chars 검증 / `generate-template.js` 12,000 자 단일 호출 한도를 chunking 으로 대체 / 동일 블록 5 회 LLM 적응 시 인용 표기 100 % 보존, 수치 환각 0
  - **단계 ④ AI 매핑 (compact index)**: LLM 에 보내는 페이로드 크기 ≤ 50 KB (`templates_index.json` 만) / 매핑 결과는 후보 ID 리스트만 반환 / 본문은 클라이언트 조립 / 매핑 정확도 사람 평가 70 % 이상
  - **단계 ⑤ Tier 3 ASCII**: 4 주력 타입 각 10 회 생성 → **초기 80 % 즉시 사용 가능 + 1 차 이터레이션 후 90 %** (deterministic 우선, LLM 폴백은 부차) / regression fixture 통과
  - **단계 ⑥ Tier 2 (c) 풀세트**: 4 적응 모드 (a·b·c·d) 모두 동작 / 톤 일관성 사람 평가 80 점 이상 / 평가 데이터셋 (블록 30-50 종 × 도메인 3-6 종) 회귀 통과

---

## 부록. 관련 작업로그 / 메타 자산 cross-reference

- `작업로그.md` 엔트리 #58 — 본 진단의 수행 과정·판단 근거·6 에이전트 조사 결과·방법론 후보 4.82·4.83·4.84 + **사용자 점검 반영 변경 이력**
- `작업로그.md` 엔트리 #61 — Tier 2(a) 보안 프록시 최소 구현 결과 (Worker·client·UI·테스트)
- `방법론_총론.md` §3 — 4.30 (에이전트 안전 영역) · 4.34 (한국어 압축) · 4.19 (톤 일관성) · 4.1 (모듈 재사용)
- `운영_모드_Quickstart.md` — Tier 1 위저드가 대체할 현 5 단계 수동 워크플로
- `양식검증_DX촉진_R&D.md` — 양식 스키마 확충 시 복제할 8 섹션 표준 (DX촉진 1 종 만 머신 파서블 상태)

### 변경 이력

| 일자 | 변경 | 사유 |
|---|---|---|
| 2026-05-12 (초안) | §1 "자동화 인프라 80 % 가 이미 존재" + Tier 1·2·3 일정 1-2/2-4/2-3 주 + Tier 1 우선 순서 | 1·2 차 Explore 6 에이전트 조사 결과 |
| 2026-05-12 (linter) | Tier 2 → "Gemini + Cloudflare LLM 엔진 레이어" 재명명 + 아키텍처 다이어그램 + AI Gateway 설정 + §5 비용 표 재구성 | 사용자 LLM/배포 결정 (Gemini + Cloudflare Worker + Gateway) |
| 2026-05-12 (사용자 점검 반영) | §1 "70-80 % 콘텐츠 원료 + UI" 로 정정 / §2 LLM row 단계 산정 / §3.0 권장 진행 순서 신규 (Tier 2 보안 프록시 우선) / Tier 2 (a)/(b)/(c) 단계 분리 + Worker 공격 표면·chunking·compact index·.gitignore 보강 / Tier 3 일정 2-4 주 + 80→90 % 단계화 / §6 위험 5 항 추가 (8 → 13) / §7 검증 파일 line 387-389·1.2 MB·5 공식 출처 추가 / §8 결론 일정 6-10 / 10-14 주 재산정 / §9 검증 6 단계화 | 사용자 점검 의견 — 현실성 7/10, "80 % 완성" 과장, Tier 2 보안 프록시 우선, 모델 deprecation, 공개 표면, chunking, compact index, .gitignore. Cloudflare/Google 공식 문서 5 링크 출처 |
| 2026-05-12 (구현 ①) | Tier 2(a) 최소 구현 완료 상태 반영 — Worker `/api/llm`, `llm-client.js`, Worker endpoint UI, `.dev.vars*`·`.env*` 차단, 12,000 자 Phase 1 한도, `wrangler.toml` 추적 정책 정정 | 사용자 "Implement the plan." 실행 결과와 문서 정합성 유지 |
