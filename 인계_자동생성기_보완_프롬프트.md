# 인계 프롬프트 — 사업계획서 자동 생성기 종합 보완

> 본 문서는 **다른 Claude Code 세션 또는 에이전트가 단독으로 인계받아 작업을 마무리할 수 있도록 설계된 self-contained 인계 프롬프트** 다. 이 .md 한 파일만 읽으면 작업 컨텍스트·이미 완료된 변경·남은 작업·검증 기준을 모두 알 수 있다.
>
> **사용 방법**: 새 Claude Code 세션의 첫 메시지로 본 .md 의 §"작업 시작 명령" 섹션을 복사·붙여넣기 하거나, "/Volumes/EXDATA/temp_git/ai-docs-for-biz/인계_자동생성기_보완_프롬프트.md 를 읽고 §8 작업 시작 명령에 따라 진행해 줘" 라고 지시한다.

---

## 0. 인계 메타

- **인계 일시**: 2026-05-13 (이전 세션 종료 시점)
- **워크스페이스**: `/Volumes/EXDATA/temp_git/ai-docs-for-biz`
- **연계 plan 파일**: `/Users/lanco/.claude/plans/ai-curious-quasar.md` (참고용, 본 .md 가 self-contained 라 plan 안 읽어도 됨)
- **연계 작업로그**: 워크스페이스 root `작업로그.md` 엔트리 #78 + 미결 §5.7 G1~G4
- **이전 세션 작업 분량**: Phase 1 의 60 % 정도 (코드 변경 완료 + 단위 테스트 작성 + 2 fail 진단까지)

---

## 1. 컨텍스트 (왜 이 작업인가)

워크스페이스의 사업계획서 자동 생성기는 **이미 70~75 % 구현된 상태** 다 — Cloudflare Worker (`worker/src/agent.js` 461 줄, 9 섹션 병렬 Gemini 호출) + Pages UI (`docs/agent.md` + `docs/javascripts/agent-ui.js` 5 단계 입력폼) + LAYER A (`worker/src/library.js`, deterministic 9 섹션 조립) + lint (`tools/lint_plan.py` 6 축 검사) 가 모두 작동 중이다.

**남은 4 gap**:
1. **신규 5 도메인 (CAS·HEA·PLT·SHP·ASM)** 이 코드에 미반영 (직전 세션 #78 의 미결 G1) — **이전 세션에서 코드는 반영 완료, 테스트 일부 fail 진단까지**.
2. **scale → 정량 슬롯 22 종 자동 매핑** 부재 — **이전 세션에서 SCALE_PROFILE + buildSlots fallback 추가 완료, 단 사용자 명시 입력 우선 로직에 1 fail**.
3. **로컬 CLI (`tools/generate_plan.py`)** 부재 — **미착수**.
4. **자동 audit (생성 직후 6 축 검증 + UI 표시)** 부재 — **미착수**.

**목표**: 위 4 gap 메움 → 자동 생성기 완성도 70 → 95 % + Worker 경로와 로컬 CLI 경로가 같은 입력에 같은 9 섹션 출력 + 동일 audit 6 축 PASS 보장.

---

## 2. 이전 세션이 완료한 것 (코드 변경 + 단위 테스트)

### 2.1 `worker/src/library.js` 변경 (적용 완료)

- **DOMAIN_PROFILE 에 신규 5 도메인 추가** (CAS·HEA·PLT·SHP·ASM). 13 항목 (process_default + 12 sample/examples) 매트릭스. 입력 자료 = `사전_슬롯과_도메인_10종.md` §2.2.
- **SCALE_PROFILE 객체 신설** (한글 키 '중소'·'중견'·'대기업' + 영문 alias small/mid/large). 정량 슬롯 10 종 (total_budget_eok·govt_ratio_pct·private_ratio_pct·duration_months·trl_start·trl_target·veteran_count·spec_variance_pct·pdf_form_count·retention_period).
- **`pickScale(scale)` helper** 추가 (alias normalize).
- **`buildSlots()` 확장** — `profile.scale` 있으면 `pickScale` 결과를 DEFAULTS 직후에 spread (사용자 명시 입력 우선 처리는 미구현 — fail 2 의 원인).

### 2.2 `worker/src/agent.js` 변경 (적용 완료)

- **`DOMAIN_BY_INDUSTRY` 에 신규 5 도메인 추가** + 각 도메인 패키지·시나리오 ID 매핑 (CAS=pkg1, HEA=pkg3, PLT=pkg5, SHP=pkg1, ASM=pkg5).

### 2.3 `worker/test/library.test.js` 신설 (적용 완료, 56 케이스)

- **30 케이스**: 10 도메인 × 3 규모 = `buildSlots` 가 13 도메인 어휘 + 10 규모 정량 슬롯을 채우는지.
- **4 케이스**: scale fallback 동작 (미지정/영문 alias/사용자 명시 우선/알 수 없는 scale).
- **10 케이스**: 10 도메인 × `composeFromLibrary` 9 섹션 헤더 시작 + 슬롯 잔존 0.
- **2 케이스**: STL/CAS 도메인 §1 첫 단락의 facility·product 어휘 회귀 검증.

### 2.4 테스트 실행 결과 (이전 세션 마지막)

```
# tests 56  # pass 54  # fail 2
```

---

## 3. 발견된 2 FAIL — 원인 분석 + 수정 지침

### Fail 1: `worker/test/index.test.js:234` "uses Gemini section writers when requested"

- **에러 메시지**: `The input did not match the regular expression /BLK-COMPANY-01/`
- **원인 분석**: `agent.js:130 buildSectionPrompt` 의 LAYER A 라이브러리 분기 (libraryBase 가 있을 때) 가 prompt 본문에 `BLK-COMPANY-01` 같은 BLK ID 를 명시하지 않는다. 기존 테스트는 LLM path 의 prompt 에 BLK-XXX-NN 이 포함되리라 기대했지만, libraryBase 분기는 §1·§2 등 LAYER A 본문만 base 로 제공한다.
- **본 plan 변경과의 관계**: **무관할 가능성이 높다**. libraryBase 분기는 작업로그 #77 (커밋 b3a21c2) 에서 도입된 것이고, 본 인계 작업의 변경 (DOMAIN_PROFILE + SCALE_PROFILE 추가) 과는 무관. **검증**: 본 변경 전·후를 비교해야 — `git stash && cd worker && npm test` 후 fail 동일하면 회귀가 아닌 사전 존재 fail.
- **권장 수정 (3 옵션 중 1)**:
  - (a) `agent.js:130 buildSectionPrompt` 의 libraryBase 분기 prompt 에 `- 출처 블록: ${outlineSection.guide}` 한 줄 추가 (BLK ID 명시).
  - (b) 테스트 기대값 수정 — libraryBase 분기에서는 BLK 명시 안 하는 게 의도라면 `index.test.js:234` 의 `assert.match(fetchCalls[0].prompt, /BLK-COMPANY-01/)` 를 `assert.match(fetchCalls[0].prompt, /§1 현황/)` 또는 `/LAYER A 라이브러리/` 로 교체.
  - (c) git blame 으로 #77 이전·이후 동일 fail 확인 → 사전 존재면 본 인계 작업 scope 외로 분리 (별 후속 작업).
- **추천**: (c) → (a). git log 로 사전 존재 확인 후, 사전 존재면 본 plan 외 별 단계로 분리. 신규 회귀면 (a) 로 prompt 보강.

### Fail 2: `worker/test/library.test.js:87` "사용자 명시 입력이 scale fallback 보다 우선"

- **에러 메시지**: `Expected values to be strictly equal: '22' !== '50'`
- **원인 분석**: `library.js buildSlots` 의 spread 순서가 `{ ...DEFAULTS, ...scaleProfile, company, industry, ... }` 로 끝나서, 사용자가 `profile.total_budget_eok='50'` 으로 명시해도 buildSlots 가 그 키를 직접 파싱하지 않아 무시된다. scale='대기업' 의 SCALE_PROFILE 값 '22' 가 적용됨.
- **권장 수정**: `library.js buildSlots` 의 마지막 spread 직전에 사용자 정량 슬롯 추출을 추가.
  ```javascript
  const QUANT_SLOT_KEYS = [
    'total_budget_eok', 'govt_ratio_pct', 'private_ratio_pct',
    'duration_months', 'trl_start', 'trl_target',
    'veteran_count', 'spec_variance_pct', 'pdf_form_count', 'retention_period',
    'human_entry_minutes', 'human_error_pct',
    'kpi_quality_pct', 'kpi_productivity_pct', 'kpi_anomaly_accuracy_pct',
    'kpi_fp_pct', 'kpi_fn_pct',
    'ml_drift_psi_warn', 'ml_drift_psi_retrain',
    'edge_latency_ms', 'hmi_latency_s', 'rag_latency_s', 'service_sla_pct',
    'experience_years', 'variable_count',
  ];

  function pickProfileSlots(profile) {
    const out = {};
    for (const key of QUANT_SLOT_KEYS) {
      if (profile?.[key] !== undefined) out[key] = String(profile[key]);
      else if (profile?.step1_company?.[key] !== undefined) out[key] = String(profile.step1_company[key]);
      else if (profile?.step2_business?.[key] !== undefined) out[key] = String(profile.step2_business[key]);
    }
    return out;
  }
  ```
  그리고 `buildSlots` 의 return 객체에 `...pickProfileSlots(profile)` 를 마지막에 추가 (DEFAULTS·scaleProfile 보다 우선).

---

## 4. 남은 작업 — Phase 1 마무리 + Phase 2~5 (산출물 5 종)

### Phase 1 마무리 (2 fail 0 까지)

1. Fail 1: git log·git diff 로 사전 존재 회귀인지 확인 → 사전 존재면 작업로그 미결에 별 단계로 추가, 신규면 prompt 보강.
2. Fail 2: 위 §3 의 권장 수정 (`pickProfileSlots` helper 추가) 적용.
3. `npm test` 56 PASS 확인.
4. 작업로그 #79 (Phase 1 완료) 엔트리 추가 — 8 필드 양식.

### Phase 2 — `worker/src/audit.js` 신설 + agent.js 통합

**산출물**: `worker/src/audit.js` (~150 줄, JS), `worker/test/audit.test.js` (~80 줄).

**audit.js 의 6 축 검사** (= `tools/lint_plan.py` JS port):

```javascript
// 6 축 검사 함수 export
export function runAudit(finalMd, industry) {
  return {
    passed: /* 6 축 모두 pass 인지 */,
    checks: {
      slot:           checkSlotResidual(finalMd),       // {{slot_*}} 잔존 0
      placeholder:    checkPlaceholderResidual(finalMd), // [수치]·[고객사] 등 화이트리스트 외 잔존 0
      sectionHeaders: checkSectionHeaders(finalMd),     // ## §1~§9 누락 검사
      balance:        checkSectionBalance(finalMd),     // 섹션별 줄 수 균형도 ≥ 0.10
      cross:          checkDomainCrossPollution(finalMd, industry), // 다른 도메인 어휘 누출 < 4
      meta:           checkMetaLeakage(finalMd),        // TODO·sample_·DEFAULTS·buildSlots 누설 0
    },
  };
}
```

**DOMAIN_VOCAB** (10 도메인 × 5 단어, lint_plan.py 와 동일):

```javascript
const DOMAIN_VOCAB = {
  STL: ['1ZHM', '2ZHM', 'BAF', '압연유', '스테인리스'],
  MET: ['CNC', '머시닝센터', '절삭 토크', '공구 마모'],
  RUB: ['밴버리', '가황', '압출', '고무 배합'],
  UTL: ['보일러', '압축기', '냉동기', '폐수 처리', 'SOx', 'NOx'],
  LLM: ['RAG', '환각률', '임베딩', 'BGE', 'KoAlpaca'],
  CAS: ['턴디시', '몰드', '연주', '용강', '슬라브'],
  HEA: ['가열로', 'QT', '퀜칭', '템퍼링', '결정립'],
  PLT: ['도금조', '전기도금', 'QUALICOAT', '분체도장', '정류기'],
  SHP: ['선각', '블록', '선급', 'DNV', 'ABS', '용접 비드'],
  ASM: ['체결력', '산업로봇', '토크 건', '비전 검사'],
};
```

**`tools/lint_plan.py` 의 정확한 패턴 참조** (line 27~67) — slot 정규식 `\{\{(\w+)\}\}`, placeholder 정규식 `\[(고객사|공정|수치|기간|%)\b[^\]]*\]`, 화이트리스트 `\[고객사 — [^\]]+\]`, 메타 패턴 `\bTODO\b`·`\bsample_\w+`·`\bDEFAULTS\b` 등.

**`agent.js` 통합**:

- `agent.js` 의 9 섹션 조립 완료 직후 (현재 약 `compileFinalMarkdown` 직후) `import { runAudit } from './audit.js'` → `const audit = runAudit(final_md, plan.domain)` → SSE 응답 객체에 `audit` 필드 추가.
- 기존 `complete` 이벤트 데이터에 `audit: { passed, checks }` 키 추가.
- `compileAuditMarkdown` 에 6 축 결과 표를 추가하는 것도 가능 (선택).

**`worker/test/audit.test.js`**:

- 입력: `사업계획서_가상_10종/*.md` 10 파일을 fs.readFile 로 읽어 runAudit 호출.
- 기대: 10 파일 모두 `audit.passed === true` (fixture 회귀).
- 추가: 의도적으로 슬롯 잔존·헤더 누락 케이스를 만들어 fail 잡는지 검증 (negative test 2~3 개).

### Phase 3 — UI 갱신 + Cloudflare 재배포

**산출물**: `docs/agent.md` 수정, `docs/javascripts/agent-ui.js` 수정, Cloudflare Worker·Pages 재배포.

**agent.md** (line 32~36 부근의 도메인 dropdown):

- 기존 5 옵션 (STL·MET·RUB·UTL·LLM) 에 **CAS·HEA·PLT·SHP·ASM 5 옵션 추가** = 10 항목.
- 사용자 친화 레이블: "CAS — 연속주조·중력주조", "HEA — 열처리·QT", "PLT — 도금·표면처리", "SHP — 조선·해양기자재", "ASM — 자동차부품 조립".

**agent-ui.js** SSE 핸들러:

- 기존 `complete` 이벤트 처리 부분에 `data.audit` 필드 수신 후 화면 표시 영역 신설 — `audit-panel` div 에 6 축 ✅/❌ 매트릭스 렌더.
- audit 리포트 다운로드 버튼 추가 (final_md·audit_md 다운로드 버튼 옆).

**재배포**:

```bash
cd worker
npm run deploy:dry-run  # 1 차 검증
npm run deploy          # 실제 배포
# Pages 는 GitHub push 시 자동 재배포 (.github/workflows/deploy.yml)
```

**공개 검증**: `https://ai-docs-for-biz.pages.dev/agent/` 에서 신규 5 도메인 (CAS·HEA·PLT·SHP·ASM) 각각 1 회 생성 → SSE `audit.passed === true` 확인.

### Phase 4 — `tools/generate_plan.py` 로컬 CLI 신설

**산출물**: `tools/generate_plan.py` (~250 줄, Python 표준 라이브러리만).

**CLI 인터페이스**:

```bash
python3 tools/generate_plan.py \
    --domain CAS \
    --scale 대기업 \
    --company "[고객사 — 가상 대기업 일관제철 연주·압연 통합사 F]" \
    --process "연속주조·중력주조·전기로 출강" \
    --scenarios SCN-CAS-01,SCN-CAS-02,SCN-CAS-04,SCN-MLO-01 \
    --package pkg1 \
    --output 사업계획서_가상_10종/06_CAS_대기업.md
# 생성 후 자동 lint
# --no-lint 옵션으로 스킵 가능
```

**동작**:

1. **입력 자료 읽기** — `라이브러리_공통문구_9섹션.md` (LAYER A 본문 권위) + `사전_슬롯과_도메인_10종.md` (10 도메인 매트릭스 + 슬롯 사전).
2. **마크다운 파싱** — `## §N` 헤더 분할 + 단락·서브섹션 추출. 정규식 `^## §(\d)` 로 9 섹션 분할.
3. **슬롯 채움** — `{{(\w+)}}` 정규식 치환 (library.js fill() 함수 동일 동작).
4. **도메인 어휘 결정** — 사전 §2.1·§2.2 의 도메인 매트릭스를 dict 로 파싱 → 사용자 --domain 으로 1 행 픽업 → 12 도메인 어휘 슬롯 채움.
5. **scale 정량 슬롯 결정** — 사전 §1.3 의 권장 범위 또는 SCALE_PROFILE 동등 (library.js SCALE_PROFILE 의 Python 복사) 으로 10 정량 슬롯 채움. 사용자 명시 입력 우선.
6. **출력** — 머리말 (1 줄 사업명) + §1~§9 + 조립 메타 표.
7. **자동 lint** — subprocess 로 `python3 tools/lint_plan.py <output>` 호출, stderr 출력. exit 0 이면 PASS, 아니면 사용자 알림.

**Python 표준 라이브러리만**: argparse·re·subprocess·pathlib·sys 등. PyYAML 사용 안 함 (사전이 마크다운 표 형태라 정규식 파싱).

**사용자 권장**: TDD 로 진행 — `tools/test_generate_plan.py` 먼저 작성 (10 도메인 × 3 규모 = 30 케이스), 그 다음 generate_plan.py 구현. unittest 사용.

### Phase 5 — E2E 검증 + 회귀 + 작업로그

1. **Worker 경로 audit 통과** — `https://ai-docs-for-biz.pages.dev/agent/` 에서 10 도메인 각각 생성 → audit 6 축 ✅ × 10.
2. **CLI 경로 audit 통과** — `python3 tools/generate_plan.py --domain X --scale Y` × 30 케이스 → exit 0 + lint 표 6 축 ✅.
3. **dual-path 동등성** — 같은 입력 (industry·scale·company·process·scenarios·package) 으로 Worker · CLI 양 경로 출력 → diff (deterministic path 만, LLM path 비교 제외) 가 머리말·메타 제외 0.
4. **회귀** — 직전 세션의 `사업계획서_가상_10종/*.md` 10 파일과 CLI 신규 출력 → diff 가 머리말·메타 제외 0.
5. **단위 테스트** — `cd worker && npm test` → 0 fail.
6. **작업로그 #79 엔트리** — 8 필드 양식 (맥락·요청·수행·근거·결정·산출물·배움·다음).
7. **방법론 4.40** — "dual-path 동등성 검증" (Worker · CLI 가 같은 본문 자료에서 derive 되는 경우의 검증법) 을 `방법론_총론.md` §3 + `작업로그.md` §4 동시 추가.
8. **미결 G1·G4 ✅ 완료 표시** — 작업로그.md §5.7.

---

## 5. 재사용 자산 위치 (덮어쓰지 않음)

| 자산 | 위치 | 역할 | 변경 여부 |
|---|---|---|---|
| `worker/src/library.js` | LAYER A 권위 | DOMAIN_PROFILE·SCALE_PROFILE | **이전 세션 변경 적용됨** |
| `worker/src/agent.js` | 9 섹션 조립 + SSE | DOMAIN_BY_INDUSTRY 확장 | **이전 세션 변경 적용됨** |
| `worker/src/audit.js` | 신설 예정 | 6 축 검사 JS port | Phase 2 신설 |
| `worker/src/index.js` | SSE 라우팅 | audit 이벤트 송출 | Phase 2 수정 |
| `worker/src/gemini.js` | LLM 호출 | (변경 없음) | — |
| `worker/test/library.test.js` | 단위 테스트 | 30 + 4 + 12 케이스 | **이전 세션 신설** |
| `worker/test/index.test.js` | 통합 테스트 | (Fail 1 진단 필요) | (변경 자제) |
| `worker/test/audit.test.js` | 단위 테스트 | 신설 예정 | Phase 2 신설 |
| `tools/lint_plan.py` | 6 축 검사 Python 권위 | audit.js JS port 의 reference | **변경 없음** |
| `tools/generate_plan.py` | 로컬 CLI | 신설 예정 | Phase 4 신설 |
| `라이브러리_공통문구_9섹션.md` | LAYER A 마크다운 view | CLI 입력 자료 | **변경 없음** |
| `사전_슬롯과_도메인_10종.md` | 슬롯·도메인 사전 | CLI 입력 자료 | **변경 없음** |
| `사업계획서_가상_10종/01~10*.md` | 회귀 검증 견본 | CLI/audit 회귀 비교 | **변경 없음** |
| `점검_체크리스트_가상10종.md` | 사람 검수 매트릭스 | (참고) | — |
| `가이드_조립_공통과수시.md` | 조립 절차 표준 | CLI 설계의 reference | — |
| `docs/agent.md` | Pages UI | 도메인 dropdown 5→10 | Phase 3 수정 |
| `docs/javascripts/agent-ui.js` | UI 스크립트 | audit 표시 추가 | Phase 3 수정 |
| `wrangler.toml` | Worker 배포 | (변경 없음) | — |
| `작업로그.md` | 진행 연대기 | #79 엔트리 + 방법론 4.40 + G1·G4 완료 표시 | Phase 5 갱신 |

---

## 6. 검증 기준 (end-to-end)

1. ✅ **이전 세션의 2 fail 모두 해결** — `cd worker && npm test` → 0 fail.
2. ✅ **Phase 2 audit.js** — `worker/test/audit.test.js` 통과 (가상_10종 10 파일 모두 `audit.passed === true` + negative test 2~3 PASS).
3. ✅ **Phase 3 UI 재배포** — 공개 URL 에서 신규 5 도메인 (CAS·HEA·PLT·SHP·ASM) 각각 SSE `complete.audit.passed === true`.
4. ✅ **Phase 4 CLI** — 30 케이스 (10 도메인 × 3 규모) 실행 + 모두 exit 0 + lint 표 6 축 ✅.
5. ✅ **dual-path 동등성** — Worker · CLI 같은 입력 출력 diff 0 (머리말·메타 제외).
6. ✅ **가상_10종 회귀** — CLI 출력 vs 직전 세션 가상_10종/*.md diff 0 (머리말·메타 제외).
7. ✅ **작업로그 #79** + **방법론 4.40** + **G1·G4 ✅ 표시**.

---

## 7. 의도적으로 하지 않을 것 (scope 분리)

- **시나리오_카탈로그.md 신규 25 시나리오 정식 등록** (G2) — 본 plan UI 의 SCENARIOS dropdown 은 후보만 노출, 카탈로그 정식 등록은 별 단계.
- **LAYER B Gemini 출력 품질 측정** (G4 본체) — 본 plan 의 audit 가 첫 입력 metric 이 되지만, Gemini 출력 통계 분석은 별 단계.
- **점검 체크리스트 매트릭스 사람 채움** (G3) — 자동 audit 까지. 300 셀 사람 채움은 별 단계.
- **다국어 출력** — LIBRARY 객체 다국어화 필요. 한국어 only.
- **Worker SLA·rate limit 고도화** — Free tier cap (AGENT_MAX_LLM_SECTIONS=5) 유지.
- **사용자 인증·과금** — 본 plan 외.
- **agent.js buildSectionPrompt libraryBase 분기 prompt 본문 변경** — Fail 1 의 (a) 옵션은 sub-scope, 사전 존재 회귀면 별 단계로 분리.

---

## 8. 작업 시작 명령 (인계받은 에이전트가 첫 turn 에 할 일)

다음을 순서대로 수행한다.

1. **CLAUDE.md 읽기** — `cat /Volumes/EXDATA/temp_git/ai-docs-for-biz/CLAUDE.md` (워크스페이스 규약·작업로그 갱신 의무).
2. **작업로그 직전 엔트리 #78 + 미결 §5.7 확인** — `grep -A 50 "엔트리 #78" 작업로그.md` 와 `grep -A 30 "5.7 4 분할" 작업로그.md`.
3. **현재 코드 상태 확인** — `cd worker && npm test 2>&1 | tail -20` 실행 → 56 / 54 PASS / 2 FAIL 재현. 실패 메시지가 본 .md §3 의 두 fail 과 일치하는지 검증.
4. **Fail 1 사전 존재 회귀 확인** — `git log --oneline worker/src/agent.js | head -5` + `git stash && cd worker && npm test` 후 fail 동일하면 사전 존재. 회복: `git stash pop`.
5. **Fail 2 수정 적용** — 본 .md §3 Fail 2 의 권장 수정 (`pickProfileSlots` helper) 을 `worker/src/library.js` 에 적용 → npm test 재실행 → fail 0.
6. **사용자에게 진행 보고** — Fail 1 의 사전 존재 여부와 권장 처리 (별 단계 분리 vs (a) 옵션 적용) 를 보고하고 결정 받기.
7. **사용자 결정 후 Phase 2 시작** — `worker/src/audit.js` 신설부터 본 .md §4 Phase 2~5 순서대로 진행.
8. **각 Phase 종료 시 작업로그 8 필드 엔트리 추가** — 방법론 4.29 의무. 엔트리 번호는 #79 부터.
9. **Phase 5 종료 후 git commit** — 사용자 명시 승인 시에만. Conventional Commits + 양 원격 push (방법론 4.27).

---

## 9. 빠른 참조 — 자주 쓸 명령

```bash
# 워크스페이스 진입
cd /Volumes/EXDATA/temp_git/ai-docs-for-biz

# Worker 테스트
cd worker && npm test
cd worker && npm test 2>&1 | grep -B1 -A 12 "not ok"

# Worker 배포 검증·실배포
cd worker && npm run deploy:dry-run
cd worker && npm run deploy

# Lint 실행
python3 tools/lint_plan.py 사업계획서_가상_10종/

# CLI 사용 예 (Phase 4 완성 후)
python3 tools/generate_plan.py --domain CAS --scale 대기업 \
    --company "[고객사 — 가상 ...]" --process "연속주조" \
    --scenarios SCN-CAS-01,SCN-CAS-02 --package pkg1 \
    --output /tmp/test_CAS.md

# 작업로그 마지막 엔트리 확인
grep -nE "^#### 엔트리 #" 작업로그.md | tail -3

# git status
git status; git log --oneline -5
```

---

## 10. 인계자에게 묻고 싶은 것이 있다면

본 .md 가 self-contained 이지만, 다음 상황에서는 **사용자에게 명시적으로 묻기**:

- Fail 1 이 사전 존재 회귀로 확인되었을 때: "별 단계로 분리할까, 아니면 본 plan 안에서 함께 처리할까?"
- Phase 3 Cloudflare 재배포 직전: "재배포 진행해도 되나? `wrangler deploy` 는 destructive operation."
- Phase 5 git commit 직전: "Conventional Commits + 양 원격 push 진행해도 되나?"

그 외는 본 .md 의 권장 절차대로 자율 진행.

---

> [출처]
> - 본 인계 프롬프트: 2026-05-13 이전 세션 plan + 코드 변경 + 2 fail 진단 결과
> - plan 파일: `/Users/lanco/.claude/plans/ai-curious-quasar.md`
> - 워크스페이스 컨벤션: `CLAUDE.md` + `작업로그.md` 1.2 (8 필드) + 방법론 4.26·4.27·4.29
> - 직전 세션 산출물: 작업로그 엔트리 #78 + 사전·라이브러리·가상 10 종·체크리스트·lint 5 자료 (이미 root 에 존재)
